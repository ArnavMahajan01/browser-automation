# 01 — Project Scaffold

**Date:** Sep 6, 2026

## What was set up

- Next.js 16.2.6 project initialised with React 19
- Tailwind CSS v4 configured via `@tailwindcss/postcss`
- shadcn/ui component library added (`components/ui/` populated with ~50 components)
- `ThemeProvider` added (`components/theme-provider.tsx`) with dark mode toggle via `next-themes`
- `Geist` and `Geist Mono` fonts wired into the root layout via CSS variables
- Base `app/layout.tsx` and `app/page.tsx` scaffolded
- `lib/utils.ts` with `cn()` helper
- `AGENTS.md` added with Next.js agent guidance rules
